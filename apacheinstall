pipelines {
  agent any {
       stages {
        stages ('install apache') {
        steps {
             sh 'sudo apt install apache2-bin'
             sh 'apache2 -version'
            }
         }
      }
 }
}
