pipeline {
      agent any 
    stages {
        stage ('install apache') {
          steps {
             //sh 'sudo apt install apache2-bin'
             sh 'apache2 -version'
            }
         }
      }
 }

